function setup() {
  createCanvas(1000, 1000);
}

function draw() {
  ellipse(500, 500, 1000, 550);
  ellipseMode(CENTER);
  fill(0);
  ellipse(500, 500, 900, 500);
  ellipseMode(CENTER);
  fill(255);
  ellipse(500, 500, 800, 450);
  ellipseMode(CENTER);
  fill(0);
  ellipse(500, 500, 700, 400);
  ellipseMode(CENTER);
  fill(255);
  ellipse(500, 500, 600, 350);
  ellipseMode(CENTER);
  fill(0);
  ellipse(500, 500, 500, 300);
  ellipseMode(CENTER);
  fill(255);
  ellipse(500, 500, 400, 250);
  ellipseMode(CENTER);
  fill(0);
  ellipse(500, 500, 300, 200);
  ellipseMode(CENTER);
  fill(255);
  ellipse(500, 500, 200, 150);
  ellipseMode(CENTER);
  fill(0);
  ellipse(500, 500, 100, 100);
  ellipseMode(CENTER);
  fill(255);

  ellipse(100, 100, 80, 80);
  ellipseMode(CENTER);
  fill(255);
  ellipse(800, 800, 80, 80);
  ellipseMode(CENTER);
  fill(0);
  ellipse(100, 800, 80, 80);
  ellipseMode(CENTER);
  fill(0);
  ellipse(800, 100, 80, 80);
  ellipseMode(CENTER);
  fill(0);
}
